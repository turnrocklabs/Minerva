name: Build Project

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform to build for'
        required: true
        default: 'all'
        type: choice
        options:
        - all
        - windows
        - linux
        - macos
      create_release:
        description: 'Create GitHub Release'
        required: false
        default: true
        type: boolean
  push:
    paths:
      - "src/**"
    branches:
      - development

# Single source of truth for the Godot version — every job that installs Godot
# reads these. Keep in sync with the project's config/features in project.godot.
env:
  GODOT_VERSION: "4.6.2"
  GODOT_RELEASE: "stable"

jobs:
  check-gdextension:
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.check.outputs.changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - id: check
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            git diff --name-only HEAD^ HEAD > changes.txt
            if grep -q "src/gdextension/" changes.txt; then
              echo "changed=true" >> $GITHUB_OUTPUT
            else
              echo "changed=false" >> $GITHUB_OUTPUT
            fi
          fi

  build-windows:
    needs: check-gdextension
    if: (github.event.inputs.platform == 'windows' || github.event.inputs.platform == 'all' || github.event_name == 'push')
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
        lfs: true
    
    - name: Cache SCons build
      uses: actions/cache@v4
      with:
        path: |
          src/.sconsign.dblite
          src/**/*.os
          src/**/*.obj
        key: ${{ runner.os }}-scons-${{ hashFiles('src/**/*.cpp', 'src/**/*.h') }}
        restore-keys: |
          ${{ runner.os }}-scons-

    - name: Cache godot-cpp
      uses: actions/cache@v4
      with:
        path: |
          src/godot-cpp/bin/**
          src/godot-cpp/gen/**
        key: ${{ runner.os }}-godot-cpp-${{ hashFiles('src/godot-cpp/**/*.cpp', 'src/godot-cpp/**/*.h') }}
        restore-keys: |
          ${{ runner.os }}-godot-cpp-
        
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Apply godot_wry patches
      shell: bash
      run: |
        cd vendor/godot_wry
        git checkout -- .
        for patch in ../../patches/godot_wry-*.patch; do
          [ -f "$patch" ] && git apply "$patch" && echo "Applied: $patch"
        done

    - name: Build godot_wry
      working-directory: vendor/godot_wry/rust
      run: cargo build --release

    - name: Install godot_wry binary
      shell: bash
      run: |
        mkdir -p src/addons/godot_wry/bin/x86_64-pc-windows-msvc
        cp vendor/godot_wry/rust/target/release/godot_wry.dll src/addons/godot_wry/bin/x86_64-pc-windows-msvc/

    - name: Setup Python and SCons
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
    - run: pip install scons

    - name: Build Extension
      run: |
        cd src
        scons platform=windows target=template_release
        scons platform=windows target=template_debug
        
    - name: Upload Extension
      uses: actions/upload-artifact@v4
      with:
        name: terminal-windows
        path: src/bin/libterminal*

    - name: Upload godot_wry binary
      uses: actions/upload-artifact@v4
      with:
        name: godot-wry-windows
        path: src/addons/godot_wry/bin/x86_64-pc-windows-msvc/godot_wry.dll
        if-no-files-found: error

  build-linux:
    needs: check-gdextension
    if: (github.event.inputs.platform == 'linux' || github.event.inputs.platform == 'all' || github.event_name == 'push')
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
        lfs: true

    - name: Cache SCons build
      uses: actions/cache@v4
      with:
        path: |
          src/.sconsign.dblite
          src/**/*.os
          src/**/*.obj
        key: ${{ runner.os }}-scons-${{ hashFiles('src/**/*.cpp', 'src/**/*.h') }}
        restore-keys: |
          ${{ runner.os }}-scons-

    - name: Cache godot-cpp
      uses: actions/cache@v4
      with:
        path: |
          src/godot-cpp/bin/**
          src/godot-cpp/gen/**
        key: ${{ runner.os }}-godot-cpp-${{ hashFiles('src/godot-cpp/**/*.cpp', 'src/godot-cpp/**/*.h') }}
        restore-keys: |
          ${{ runner.os }}-godot-cpp-

    - name: Setup Zig
      run: |
        ZIG_VERSION="0.15.2"
        case "$(uname -s)-$(uname -m)" in
          Linux-x86_64)   ZIG_PLATFORM="x86_64-linux" ;;
          Linux-aarch64)  ZIG_PLATFORM="aarch64-linux" ;;
          Darwin-x86_64)  ZIG_PLATFORM="x86_64-macos" ;;
          Darwin-arm64)   ZIG_PLATFORM="aarch64-macos" ;;
        esac
        curl -L -o /tmp/zig.tar.xz "https://ziglang.org/download/${ZIG_VERSION}/zig-${ZIG_PLATFORM}-${ZIG_VERSION}.tar.xz"
        tar xf /tmp/zig.tar.xz -C /tmp
        # $GITHUB_PATH only takes effect on subsequent steps; export in-step too so the validation runs.
        ZIG_DIR="/tmp/zig-${ZIG_PLATFORM}-${ZIG_VERSION}"
        export PATH="$ZIG_DIR:$PATH"
        echo "$ZIG_DIR" >> $GITHUB_PATH
        zig version

    - name: Cache Zig build
      uses: actions/cache@v4
      with:
        path: src/gdextension/terminal/ghostty-shim/.zig-cache
        key: zig-linux-x86_64-${{ hashFiles('src/gdextension/terminal/ghostty-shim/build.zig', 'src/gdextension/terminal/ghostty-shim/src/**') }}
        restore-keys: zig-linux-x86_64-

    - name: Setup Python and SCons
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
    - run: pip install scons

    - name: Install Linux Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential util-linux

    - name: Install WRY dependencies
      run: sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Apply godot_wry patches
      run: |
        cd vendor/godot_wry
        git checkout -- .
        for patch in ../../patches/godot_wry-*.patch; do
          [ -f "$patch" ] && git apply "$patch" && echo "Applied: $patch"
        done

    - name: Build godot_wry
      working-directory: vendor/godot_wry/rust
      run: cargo build --release

    - name: Install godot_wry binary
      run: |
        mkdir -p src/addons/godot_wry/bin/x86_64-unknown-linux-gnu
        cp vendor/godot_wry/rust/target/release/libgodot_wry.so src/addons/godot_wry/bin/x86_64-unknown-linux-gnu/

    - name: Build ghostty-vt shim
      working-directory: src/gdextension/terminal/ghostty-shim
      run: zig build -Doptimize=ReleaseFast

    - name: Copy shim library to bin
      run: |
        mkdir -p src/bin
        cp src/gdextension/terminal/ghostty-shim/zig-out/lib/libminerva-vt.so src/bin/

    - name: Build Extension
      run: |
        cd src
        scons platform=linux target=template_release
        scons platform=linux target=template_debug

    - name: Upload Extension
      uses: actions/upload-artifact@v4
      with:
        name: terminal-linux
        path: |
          src/bin/libterminal*
          src/bin/libminerva-vt.so

    - name: Upload godot_wry binary
      uses: actions/upload-artifact@v4
      with:
        name: godot-wry-linux
        path: src/addons/godot_wry/bin/x86_64-unknown-linux-gnu/libgodot_wry.so
        if-no-files-found: error

  # ── Functional regression test suite (RCA 019e46b5) ──────────────────────
  # CI ran zero tests before this job. Runs the hermetic substrate tier
  # (F1-F6) — fixture-plugin tests on the real MCPServerConnection. The
  # per-plugin tier (CAD / presentation / scansort) is heavy/networked and is
  # left for a manual/nightly run: scripts/run-functional-tests.sh --all.
  # This job reports status but does not yet gate the release — wire it into
  # build-godot's `needs` once it has proven green in CI.
  functional-tests:
    needs: build-linux
    if: (github.event.inputs.platform == 'linux' || github.event.inputs.platform == 'all' || github.event_name == 'push')
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
        lfs: true

    # The hermetic tests spawn an MCP stdio subprocess, which needs the
    # SubProcess GDExtension shipped in the terminal-linux artifact.
    - name: Download terminal extension
      uses: actions/download-artifact@v4
      with:
        name: terminal-linux
        path: src/bin/

    - name: Install Linux runtime dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y pkg-config libx11-dev libxcursor-dev \
          libxinerama-dev libgl1-mesa-dev libglu-dev libasound2-dev libpulse-dev \
          libdbus-1-dev libudev-dev libxi-dev libxrandr-dev

    - name: Install Godot 4.6
      run: |
        curl -L -o ./godot.zip "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-${GODOT_RELEASE}/Godot_v${GODOT_VERSION}-${GODOT_RELEASE}_linux.x86_64.zip"
        unzip ./godot.zip
        sudo mv ./Godot_v${GODOT_VERSION}-${GODOT_RELEASE}_linux.x86_64 /usr/local/bin/godot
        godot --version

    - name: Import project
      working-directory: src
      run: |
        mkdir -p .godot
        timeout 180s godot --headless --import || true

    - name: Run functional suite (hermetic F1-F6)
      run: scripts/run-functional-tests.sh

  build-macos:
    needs: check-gdextension
    if: (github.event.inputs.platform == 'macos' || github.event.inputs.platform == 'all' || github.event_name == 'push')
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
        lfs: true

    - name: Cache SCons build
      uses: actions/cache@v4
      with:
        path: |
          src/.sconsign.dblite
          src/**/*.os
          src/**/*.obj
        key: ${{ runner.os }}-scons-${{ hashFiles('src/**/*.cpp', 'src/**/*.h') }}
        restore-keys: |
          ${{ runner.os }}-scons-

    - name: Cache godot-cpp
      uses: actions/cache@v4
      with:
        path: |
          src/godot-cpp/bin/**
          src/godot-cpp/gen/**
        key: ${{ runner.os }}-godot-cpp-${{ hashFiles('src/godot-cpp/**/*.cpp', 'src/godot-cpp/**/*.h') }}
        restore-keys: |
          ${{ runner.os }}-godot-cpp-

    - name: Setup Zig
      run: |
        ZIG_VERSION="0.15.2"
        case "$(uname -s)-$(uname -m)" in
          Darwin-x86_64)  ZIG_PLATFORM="x86_64-macos" ;;
          Darwin-arm64)   ZIG_PLATFORM="aarch64-macos" ;;
        esac
        curl -L -o /tmp/zig.tar.xz "https://ziglang.org/download/${ZIG_VERSION}/zig-${ZIG_PLATFORM}-${ZIG_VERSION}.tar.xz"
        tar xf /tmp/zig.tar.xz -C /tmp
        # $GITHUB_PATH only takes effect on subsequent steps; export in-step too so the validation runs.
        ZIG_DIR="/tmp/zig-${ZIG_PLATFORM}-${ZIG_VERSION}"
        export PATH="$ZIG_DIR:$PATH"
        echo "$ZIG_DIR" >> $GITHUB_PATH
        zig version

    - name: Cache Zig build
      uses: actions/cache@v4
      with:
        path: src/gdextension/terminal/ghostty-shim/.zig-cache
        key: zig-macos-${{ hashFiles('src/gdextension/terminal/ghostty-shim/build.zig', 'src/gdextension/terminal/ghostty-shim/src/**') }}
        restore-keys: zig-macos-

    - name: Setup Python and SCons
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
    - run: pip install scons

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Apply godot_wry patches
      run: |
        cd vendor/godot_wry
        git checkout -- .
        for patch in ../../patches/godot_wry-*.patch; do
          [ -f "$patch" ] && git apply "$patch" && echo "Applied: $patch"
        done

    - name: Build godot_wry
      working-directory: vendor/godot_wry/rust
      run: cargo build --release

    - name: Install godot_wry binary
      run: |
        mkdir -p src/addons/godot_wry/bin/universal-apple-darwin
        cp vendor/godot_wry/rust/target/release/libgodot_wry.dylib src/addons/godot_wry/bin/universal-apple-darwin/

    - name: Build ghostty-vt shim (universal)
      working-directory: src/gdextension/terminal/ghostty-shim
      run: |
        zig build -Doptimize=ReleaseFast -Dtarget=aarch64-macos
        cp zig-out/lib/libminerva-vt.dylib ../libminerva-vt-arm64.dylib
        zig build -Doptimize=ReleaseFast -Dtarget=x86_64-macos
        cp zig-out/lib/libminerva-vt.dylib ../libminerva-vt-x86_64.dylib

    - name: Create universal binary
      working-directory: src/gdextension/terminal
      run: |
        lipo -create libminerva-vt-arm64.dylib libminerva-vt-x86_64.dylib \
             -output libminerva-vt.dylib
        mkdir -p ../bin
        cp libminerva-vt.dylib ../bin/

    - name: Build Extension
      run: |
        cd src
        scons platform=macos target=template_release
        scons platform=macos target=template_debug

    - name: Upload Extension
      uses: actions/upload-artifact@v4
      with:
        name: terminal-macos
        path: |
          src/bin/libterminal*
          src/bin/libminerva-vt.dylib

    - name: Upload godot_wry binary
      uses: actions/upload-artifact@v4
      with:
        name: godot-wry-macos
        path: src/addons/godot_wry/bin/universal-apple-darwin/libgodot_wry.dylib
        if-no-files-found: error

  build-godot:
    needs: [check-gdextension, build-windows, build-linux, build-macos]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:  
          submodules: 'true'
      
      - name: Download Current Artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: terminal-*
          path: src/bin/
          merge-multiple: true

      # godot_wry is built inline in each build-* job but only its host triple's
      # binary ends up on that runner. build-godot runs on ubuntu-latest and exports
      # for all three platforms, so it needs the per-platform .dll/.so/.dylib from
      # the matching upload artifact, placed at the path WRY.gdextension references.
      - name: Download godot_wry (Windows)
        uses: actions/download-artifact@v4
        with:
          name: godot-wry-windows
          path: src/addons/godot_wry/bin/x86_64-pc-windows-msvc/

      - name: Download godot_wry (Linux)
        uses: actions/download-artifact@v4
        with:
          name: godot-wry-linux
          path: src/addons/godot_wry/bin/x86_64-unknown-linux-gnu/

      - name: Download godot_wry (macOS)
        uses: actions/download-artifact@v4
        with:
          name: godot-wry-macos
          path: src/addons/godot_wry/bin/universal-apple-darwin/

      - name: Create and Verify Bin Directory
        run: |
          mkdir -p src/bin
          ls -la src/bin/
          echo "godot_wry binaries:"
          find src/addons/godot_wry/bin/ -type f | { head -10; cat >/dev/null; }

      - name: Download EIRTeam.FFmpeg
        run: |
          FFMPEG_URL="https://github.com/EIRTeam/EIRTeam.FFmpeg/releases/download/autobuild-2025-11-12-13-44/eirteam-ffmpeg-1.1.4.zip"
          curl -L -o /tmp/ffmpeg.zip "$FFMPEG_URL"
          unzip -o /tmp/ffmpeg.zip -d /tmp/ffmpeg-extract
          FFMPEG_SRC=$(find /tmp/ffmpeg-extract -name "ffmpeg.gdextension" -exec dirname {} \; | head -1)
          for subdir in linux64 win64 macos; do
            if [ -d "$FFMPEG_SRC/$subdir" ]; then
              mkdir -p "src/addons/ffmpeg/$subdir"
              cp -r "$FFMPEG_SRC/$subdir/"* "src/addons/ffmpeg/$subdir/"
            fi
          done
          cp -r "$FFMPEG_SRC/macos/"*.framework "src/addons/ffmpeg/macos/" 2>/dev/null || true
          echo "FFmpeg addon installed"
          ls src/addons/ffmpeg/linux64/ src/addons/ffmpeg/win64/ src/addons/ffmpeg/macos/ 2>/dev/null | head -20

      # godot-sqlite ships pre-built binaries via upstream releases. scripts/build-extensions.sh
      # downloads these for local dev; CI does the same here so the addon binaries are present
      # at export time. Mirror the FFmpeg pattern above.
      - name: Download godot-sqlite addon
        run: |
          SQLITE_VERSION="v4.7"
          SQLITE_URL="https://github.com/2shady4u/godot-sqlite/releases/download/${SQLITE_VERSION}/bin.zip"
          curl -fL -o /tmp/sqlite.zip "$SQLITE_URL"
          unzip -o /tmp/sqlite.zip -d /tmp/sqlite-extract >/dev/null
          mkdir -p src/addons/godot-sqlite/bin
          cp -r /tmp/sqlite-extract/bin/* src/addons/godot-sqlite/bin/
          echo "godot-sqlite ${SQLITE_VERSION} installed"
          ls src/addons/godot-sqlite/bin/ | head -20

      # godot-cef is built by .github/workflows/godot-cef.yml on its own triggers
      # (vendor/godot_cef, patches/godot_cef/**, scripts/build-godot-cef.sh, src/addons/godot_cef/**).
      # FCIB policy keeps binaries out of git, so we pull artifacts from the latest
      # successful CEF run here. Without this step, exported builds ship without
      # CEF support and plugins with CEF panels fail to load.
      - name: Download godot-cef artifacts
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          RUN_ID=$(gh run list \
            --workflow=godot-cef.yml \
            --status=success \
            --limit=1 \
            --json databaseId \
            -q '.[0].databaseId')

          if [ -z "$RUN_ID" ]; then
            echo "::error::No successful godot-cef.yml run found. Trigger that workflow manually first."
            exit 1
          fi

          echo "Downloading godot-cef artifacts from run $RUN_ID"

          for pair in \
            "godot-cef-linux-x86_64:x86_64-unknown-linux-gnu" \
            "godot-cef-macos-universal:universal-apple-darwin" \
            "godot-cef-windows-x86_64:x86_64-pc-windows-msvc"; do
            artifact="${pair%%:*}"
            triple="${pair##*:}"
            mkdir -p "src/addons/godot_cef/bin/$triple"
            gh run download "$RUN_ID" --name "$artifact" --dir "src/addons/godot_cef/bin/$triple"
          done

          echo "CEF artifacts deployed:"
          # `find ... | head` would trip pipefail on SIGPIPE; collect first then truncate.
          find src/addons/godot_cef/bin/ -type f | { head -30; cat >/dev/null; }

      - name: Install Linux Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libx11-dev libxcursor-dev \
            libxinerama-dev libgl1-mesa-dev libglu-dev libasound2-dev libpulse-dev \
            libdbus-1-dev libudev-dev libxi-dev libxrandr-dev yasm unzip \
            libgtk-3-0 libwebkit2gtk-4.1-0
        
      - name: Install Godot
        run: |
          curl -L -o ./godot.zip "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-${GODOT_RELEASE}/Godot_v${GODOT_VERSION}-${GODOT_RELEASE}_linux.x86_64.zip"
          unzip ./godot.zip
          sudo mv ./Godot_v${GODOT_VERSION}-${GODOT_RELEASE}_linux.x86_64 /usr/local/bin/godot

      - name: Install Godot Export Templates
        run: |
          TEMPLATE_DIR="$HOME/.local/share/godot/export_templates"
          curl -L -o ./godot-templates.zip "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-${GODOT_RELEASE}/Godot_v${GODOT_VERSION}-${GODOT_RELEASE}_export_templates.tpz"
          unzip ./godot-templates.zip
          mkdir -p "$TEMPLATE_DIR/${GODOT_VERSION}.${GODOT_RELEASE}"
          mv templates/* "$TEMPLATE_DIR/${GODOT_VERSION}.${GODOT_RELEASE}"

      - name: Setup Godot Project Structure
        working-directory: src
        run: |
          mkdir -p .godot/editor
          chmod -R 777 .godot
          
      - name: Export Project
        working-directory: src
        run: |
          mkdir -p build

          timeout 20s godot --headless --import || true

          # Determine which platforms to build
          PLATFORM="${{ github.event.inputs.platform }}"

          # For push events, build all platforms
          if [ "${{ github.event_name }}" = "push" ]; then
            PLATFORM="all"
          fi

          # Check if this is the development branch
          IS_DEV_BRANCH="${{ github.ref == 'refs/heads/development' }}"

          if [ "$PLATFORM" = "windows" ]; then
            mkdir -p build/windows
            godot --headless --export-release "Windows Release" build/windows/Minerva.exe

            # Build debug version for development branch
            if [ "$IS_DEV_BRANCH" = "true" ]; then
              mkdir -p build/windows-debug
              godot --headless --export-debug "Windows Debug" build/windows-debug/Minerva.exe
            fi

          elif [ "$PLATFORM" = "linux" ]; then
            mkdir -p build/linux
            godot --headless --export-release "Linux Release" build/linux/Minerva.x86_64

            # Build debug version for development branch
            if [ "$IS_DEV_BRANCH" = "true" ]; then
              mkdir -p build/linux-debug
              godot --headless --export-debug "Linux Debug" build/linux-debug/Minerva.x86_64
            fi

          elif [ "$PLATFORM" = "macos" ]; then
            mkdir -p build/macos
            godot --headless --export-release "macOS" build/macos/Minerva.app

          else
            mkdir -p build/windows
            mkdir -p build/linux
            mkdir -p build/macos

            # For 'all' option or push events, export all three
            godot --headless --export-release "Windows Release" build/windows/Minerva.exe
            godot --headless --export-release "Linux Release" build/linux/Minerva.x86_64
            godot --headless --export-release "macOS" build/macos/Minerva.app

            # Build debug versions for development branch
            if [ "$IS_DEV_BRANCH" = "true" ]; then
              mkdir -p build/windows-debug
              mkdir -p build/linux-debug
              godot --headless --export-debug "Windows Debug" build/windows-debug/Minerva.exe
              godot --headless --export-debug "Linux Debug" build/linux-debug/Minerva.x86_64
            fi
          fi

      # ─── Post-export verification ──────────────────────────────────────
      # RCA: prior CI builds shipped binaries missing libminerva-vt.so and
      # CEF runtime data (icudtl.dat, *.pak, locales/), because Godot's
      # exporter silently drops [dependencies] entries that don't match its
      # supported shape. These checks are the durable backstop — if a future
      # extension bump adds a sidecar that the .gdextension manifest doesn't
      # advertise, the build fails red here instead of in a user's HITL.
      - name: Verify Linux build assets
        if: |
          github.event_name == 'push' ||
          github.event.inputs.platform == 'linux' ||
          github.event.inputs.platform == 'all'
        working-directory: src/build/linux
        run: |
          required_files=(
            Minerva.x86_64
            libterminal.linux.template_release.x86_64.so
            libminerva-vt.so
            libcef.so
            libgdcef.so
            gdcef_helper
            v8_context_snapshot.bin
            vk_swiftshader_icd.json
            icudtl.dat
            resources.pak
            chrome_100_percent.pak
            chrome_200_percent.pak
          )
          required_dirs=(locales)
          fail=0
          for f in "${required_files[@]}"; do
            if [ ! -f "$f" ]; then
              echo "::error::Missing required file: src/build/linux/$f"
              fail=1
            fi
          done
          for d in "${required_dirs[@]}"; do
            if [ ! -d "$d" ]; then
              echo "::error::Missing required directory: src/build/linux/$d"
              fail=1
            fi
          done
          [ "$fail" -eq 0 ] || exit 1
          echo "All required Linux runtime assets present"

      - name: Smoke-launch Linux build (extension load only)
        if: |
          github.event_name == 'push' ||
          github.event.inputs.platform == 'linux' ||
          github.event.inputs.platform == 'all'
        working-directory: src/build/linux
        run: |
          chmod +x ./Minerva.x86_64
          # Headless --quit-after exits after N frames; enough for the
          # engine to load GDExtensions and dlopen their sidecars. Won't
          # exercise CEF init (CEF only loads when a panel opens) — file
          # presence above is the CEF backstop.
          timeout 30 ./Minerva.x86_64 --headless --quit-after 30 \
              > minerva-launch.log 2>&1 || true
          if grep -E "Can't open dynamic library|GDExtension dynamic library not found|Error loading extension" minerva-launch.log; then
            echo "::error::Extension load error during smoke launch — see log below"
            cat minerva-launch.log
            exit 1
          fi
          echo "Smoke launch: no extension load errors"

      - name: Upload Windows Build
        if: |
          github.event_name == 'push' ||
          github.event.inputs.platform == 'windows' ||
          github.event.inputs.platform == 'all'
        uses: actions/upload-artifact@v4
        with:
          name: Minerva-Windows-Build
          path: src/build/windows/*
          
      - name: Upload Linux Build
        if: |
          github.event_name == 'push' ||
          github.event.inputs.platform == 'linux' ||
          github.event.inputs.platform == 'all'
        uses: actions/upload-artifact@v4
        with:
          name: Minerva-Linux-Build
          path: src/build/linux/*

      - name: Upload macOS Build
        if: |
          github.event_name == 'push' ||
          github.event.inputs.platform == 'macos' ||
          github.event.inputs.platform == 'all'
        uses: actions/upload-artifact@v4
        with:
          name: Minerva-macOS-Build
          path: src/build/macos/*

      - name: Upload Windows Debug Build
        if: |
          github.ref == 'refs/heads/development' && (
            github.event_name == 'push' ||
            github.event.inputs.platform == 'windows' ||
            github.event.inputs.platform == 'all'
          )
        uses: actions/upload-artifact@v4
        with:
          name: Minerva-Windows-Debug-Build
          path: src/build/windows-debug/*

      - name: Upload Linux Debug Build
        if: |
          github.ref == 'refs/heads/development' && (
            github.event_name == 'push' ||
            github.event.inputs.platform == 'linux' ||
            github.event.inputs.platform == 'all'
          )
        uses: actions/upload-artifact@v4
        with:
          name: Minerva-Linux-Debug-Build
          path: src/build/linux-debug/*

  create-release:
    needs: [build-godot]
    if: (github.event_name == 'workflow_dispatch' && github.event.inputs.create_release == 'true') || github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: Minerva-*-Build
          path: downloads
          
      - name: Create Release Archives
        run: |
          mkdir -p release-assets

          # Create Windows ZIP if files exist
          if [ -d "downloads/Minerva-Windows-Build" ]; then
            cd downloads/Minerva-Windows-Build
            zip -r ../../release-assets/Minerva-Windows.zip *
            cd ../..
            echo "Created Windows archive"
            ls -lh release-assets/Minerva-Windows.zip
          fi

          # Create Linux archive if files exist
          # actions/upload-artifact@v4 strips the executable bit during the
          # zip/unzip artifact roundtrip; restore it here so users don't have
          # to chmod after extracting the tarball.
          if [ -d "downloads/Minerva-Linux-Build" ]; then
            chmod +x downloads/Minerva-Linux-Build/Minerva.x86_64 \
                     downloads/Minerva-Linux-Build/gdcef_helper \
                     downloads/Minerva-Linux-Build/chrome-sandbox 2>/dev/null || true
            cd downloads/Minerva-Linux-Build
            tar -czf ../../release-assets/Minerva-Linux.tar.gz *
            cd ../..
            echo "Created Linux archive"
            ls -lh release-assets/Minerva-Linux.tar.gz
          fi

          # Create macOS ZIP if files exist (zipping the .app bundle preserves the directory structure)
          # Same artifact-roundtrip issue — chmod the .app's executable before zipping.
          if [ -d "downloads/Minerva-macOS-Build" ]; then
            find downloads/Minerva-macOS-Build -path '*/Contents/MacOS/*' -type f -exec chmod +x {} + 2>/dev/null || true
            cd downloads/Minerva-macOS-Build
            zip -r ../../release-assets/Minerva-macOS.zip *
            cd ../..
            echo "Created macOS archive"
            ls -lh release-assets/Minerva-macOS.zip
          fi

          # Create Windows Debug ZIP if files exist (development branch only)
          if [ -d "downloads/Minerva-Windows-Debug-Build" ]; then
            cd downloads/Minerva-Windows-Debug-Build
            zip -r ../../release-assets/Minerva-Windows-Debug.zip *
            cd ../..
            echo "Created Windows Debug archive"
            ls -lh release-assets/Minerva-Windows-Debug.zip
          fi

          # Create Linux Debug archive if files exist (development branch only)
          if [ -d "downloads/Minerva-Linux-Debug-Build" ]; then
            chmod +x downloads/Minerva-Linux-Debug-Build/Minerva.x86_64 \
                     downloads/Minerva-Linux-Debug-Build/gdcef_helper \
                     downloads/Minerva-Linux-Debug-Build/chrome-sandbox 2>/dev/null || true
            cd downloads/Minerva-Linux-Debug-Build
            tar -czf ../../release-assets/Minerva-Linux-Debug.tar.gz *
            cd ../..
            echo "Created Linux Debug archive"
            ls -lh release-assets/Minerva-Linux-Debug.tar.gz
          fi

          echo "Final release assets:"
          ls -la release-assets/
          
      - name: Generate release tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="manual-build-$(date +'%Y%m%d-%H%M%S')"
          else
            TAG="auto-build-$(date +'%Y%m%d-%H%M%S')"
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "Generated tag: $TAG"
          
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: "Minerva Development Build ${{ steps.tag.outputs.tag }}"
          body: |
            ## Minerva Development Build

            ⚠️ **This is a development build** - expect bugs and incomplete features!

            **Build Information:**
            - Platform: ${{ github.event.inputs.platform || 'all' }}
            - Trigger: ${{ github.event_name }}
            - Commit: ${{ github.sha }}
            - Branch: ${{ github.ref_name }}

            **Download Instructions:**

            **Release Builds (Optimized):**
            - **Windows**: Download `Minerva-Windows.zip`, extract and run `Minerva.exe`
            - **Linux**: Download `Minerva-Linux.tar.gz`, extract and run `./Minerva.x86_64`
            - **macOS**: Download `Minerva-macOS.zip`, extract `Minerva.app`. Unsigned build — clear quarantine with `xattr -dr com.apple.quarantine Minerva.app` then open.

            **Debug Builds (For Development - includes console output and debug symbols):**
            - **Windows Debug**: Download `Minerva-Windows-Debug.zip`, extract and run `Minerva.exe`
            - **Linux Debug**: Download `Minerva-Linux-Debug.tar.gz`, extract and run `./Minerva.x86_64`

            Built automatically from the development branch.
          files: release-assets/*
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}