#!/bin/sh
# Fake `go` toolchain executable for ToolchainRegistry functional tests.
# Real executable (not mocked) — spawned through the real probe path.
if [ "$1" = "version" ]; then
  echo "go version go1.22.4 linux/amd64"
  exit 0
fi
exit 1
