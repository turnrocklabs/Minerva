diff --git a/src/webkitgtk/mod.rs b/src/webkitgtk/mod.rs
index f72170b6..3229f7a7 100644
--- a/src/webkitgtk/mod.rs
+++ b/src/webkitgtk/mod.rs
@@ -538,12 +538,28 @@ impl InnerWebView {
 
       if let Some(js) = msg.js_value() {
         if let Some(ipc_handler) = &ipc_handler {
-          ipc_handler(
-            Request::builder()
-              .uri(webview.uri().unwrap().to_string())
-              .body(js.to_string())
-              .unwrap(),
-          );
+          let Some(current_uri) = webview.uri() else {
+            return;
+          };
+          let current_uri = current_uri.to_string();
+          let body = js.to_string();
+          let request = match Request::builder()
+            .uri(&current_uri)
+            .body(body.clone())
+          {
+            Ok(request) => request,
+            // `http::Uri` cannot represent file URLs with an empty authority.
+            // The Godot wrapper authenticates the capability inside the body;
+            // this URI is request metadata only and is never used for routing.
+            Err(_) if current_uri.starts_with("file:///") => {
+              let Ok(request) = Request::builder().uri("/").body(body) else {
+                return;
+              };
+              request
+            }
+            Err(_) => return,
+          };
+          ipc_handler(request);
         }
       }
     });
diff --git a/src/wkwebview/class/wry_web_view_delegate.rs b/src/wkwebview/class/wry_web_view_delegate.rs
index 3af93a6e..c829cb32 100644
--- a/src/wkwebview/class/wry_web_view_delegate.rs
+++ b/src/wkwebview/class/wry_web_view_delegate.rs
@@ -58,7 +58,21 @@ define_class!(
             CStr::from_ptr(url_utf8).to_str(),
             CStr::from_ptr(js_utf8).to_str(),
           ) {
-            ipc_handler(Request::builder().uri(url).body(js.to_string()).unwrap());
+            let body = js.to_string();
+            let request = match Request::builder().uri(url).body(body.clone()) {
+              Ok(request) => request,
+              // `http::Uri` cannot represent file URLs with an empty authority.
+              // The navigation lock authenticates the actual document URL; this
+              // request URI is metadata only and is never used for routing.
+              Err(_) if url.starts_with("file:///") => {
+                let Ok(request) = Request::builder().uri("/").body(body) else {
+                  return;
+                };
+                request
+              }
+              Err(_) => return,
+            };
+            ipc_handler(request);
             return;
           }
         }
diff --git a/src/webview2/mod.rs b/src/webview2/mod.rs
index ac2aa19a..be3235ba 100644
--- a/src/webview2/mod.rs
+++ b/src/webview2/mod.rs
@@ -817,11 +817,21 @@ impl InnerWebView {
           args.TryGetWebMessageAsString(&mut js)?;
           take_pwstr(js)
         };
-
         #[cfg(feature = "tracing")]
         let _span = tracing::info_span!(parent: None, "wry::ipc::handle").entered();
-        ipc_handler(Request::builder().uri(url).body(js).unwrap());
-
+        let request = match Request::builder().uri(&url).body(js.clone()) {
+          Ok(request) => request,
+          // Keep the real WebView2 source for the native navigation policy.
+          // This request URI is callback metadata only.
+          Err(_) if url.starts_with("file:///") => {
+            let Ok(request) = Request::builder().uri("/").body(js) else {
+              return Ok(());
+            };
+            request
+          }
+          Err(_) => return Ok(()),
+        };
+        ipc_handler(request);
         Ok(())
       })),
       token,
diff --git a/src/android/binding.rs b/src/android/binding.rs
index 8e715683..2c5ee2df 100644
--- a/src/android/binding.rs
+++ b/src/android/binding.rs
@@ -342,7 +342,17 @@ pub unsafe fn ipc(mut env: JNIEnv, _: JClass, url: JString, body: JString) {
       let url = url.to_string_lossy().to_string();
       let body = body.to_string_lossy().to_string();
       if let Some(ipc) = IPC.lock().unwrap().as_ref() {
-        (ipc.handler)(Request::builder().uri(url).body(body).unwrap())
+        let request = match Request::builder().uri(&url).body(body.clone()) {
+          Ok(request) => request,
+          Err(_) if url.starts_with("file:///") => {
+            let Ok(request) = Request::builder().uri("/").body(body) else {
+              return;
+            };
+            request
+          }
+          Err(_) => return,
+        };
+        (ipc.handler)(request)
       }
     }
     (Err(e), _) | (_, Err(e)) => {
