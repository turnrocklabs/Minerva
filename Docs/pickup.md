# Pickup — Presentation plugin

Last updated: 2026-05-04 (T7 v1 MCP authoring shipped; pivoting to MCP iteration interface for next HITL pass)

## Where I left off

Active workstream: **Presentation plugin — MCP iteration interface (v2)**.

T7 v1 MCP authoring shipped this session — 7 tools that compose-only (`presentation_create_deck`, `presentation_open_deck`, `presentation_list_slides`, `presentation_add_slide`, `presentation_set_slide_background`, `presentation_add_text_tile`, `presentation_add_image_tile`). Headless test green at 34/0. HITL pass exposed the rendering and authoring affordances; verdict: foundation works, font sizing solved with Option A coupling (font derives from tile.h ÷ line_count), but iteration is blocked because there are no modify/render/annotate tools.

User wants to author slides interactively with Claude Code as the LLM next round. That requires the **MCP iteration interface** — full plan filed.

## What changed this session (uncommitted on both repos)

### Minerva repo (`~/github/Minerva`)

- **`src/Scripts/Services/MCP/Modules/MCPPresentationTools.gd`** (NEW) — module with 7 v1 tools. Off-tree-plugin-safe (no direct `Presentation_SlideModel.x` references; treats `.mdeck` JSON as the contract). Lazy `SingletonObject` lookup via `_get_editor_pane()` to avoid class_name-scan compile errors. Live-tab + disk-mode parity via `_resolve_target` / `_commit_target`. PNG synthesis for solid_color is aspect-aware (tile rect aspect = (w/h) × slide_aspect) with floor + 4096 long-edge so STRETCH_KEEP_ASPECT_CENTERED renders edge-to-edge.
- **`src/Scripts/Services/MCP/MinervaMCPServer.gd`** — 1 line added at `_init_modules()` to register `MCPPresentationTools.new(self)`.
- **`src/test/test_mcp_presentation_tools.gd`** (NEW) — Butter Tarts headless test, 34/0 PASS. Defers all `load()` calls into a process_frame-connected callback because SceneTree._init runs before autoloads register (durable hint: `019df3b7fff871d8a063dda8c854ffdb`).

### Plugins repo (`~/github/plugins`)

- **`presentation/ui/SlideEditorPanel.gd`** —
  - Toolbar text buttons replaced with icons (UID-loaded from Minerva's `assets/icons/`); list at top of file.
  - Toolbar wrapped in a `ResponsiveContainer` (preloaded from `res://Scripts/UI/Controls/responsive_container.gd`); hides slide-counter and zoom-% labels at xs/sm widths.
  - Fullscreen preview deferred `set_slide()` via `preview.ready.connect(..., CONNECT_ONE_SHOT)` to fix null `_content_layer` at fullscreen open (preview entered tree after set_slide otherwise).
- **`presentation/ui/slide_canvas.gd`** —
  - **Option A coupling**: `_apply_tile_view_style` now derives font_px from rendered tile height: `(tile_h × 0.95) / (line_count × 1.3)`. Tile.h is the single source of truth for visual size — drag-corner-resize-the-text WYSIWYG. tile.font_size still permitted in schema for forward-compat with future fixed mode but currently ignored. Constants: `_LINE_HEIGHT_FACTOR = 1.3`, `_TEXT_INNER_PADDING_FACTOR = 0.95`.

### Docs / artifacts

- `/tmp/butter_tarts_headless.mdeck` — generated by the headless test. Brown background `#A07A4A`, ivory italic title at h=0.11, 2/3-width teal HR at y=0.19 + h=0.012, 4-bullet body at y=0.28 h=0.20, right-col solid placeholder `#C19A6B` (HITL was meant to replace with graphics-editor-generated image; deferred to v2 iteration tools).

## What to do next

### MCP iteration interface (v2)

**Plan**: `019df419ce567de0b7699b3be7b6c8b5` (parent: DCR `019dc0bbd6937264880b1327c942d5b6`).

Goal: close the iteration loop — author → render → eyeball → annotate → modify → re-render — so an LLM can interactively author slides via chat-driven MCP.

Implementation order (each is a child work_item under the plan):

| # | ID | Title | Why this slot |
|---|---|---|---|
| 1 | `019df41a2c1e72a591a6e8bc1131f99b` | Inspect: list_tiles, list_annotations, get_tile, get_slide | Nothing else can address tiles/annotations by ID without these |
| 2 | `019df41a5bda72e69785e20c090ab9d5` | modify_tile + set_slide_title + set_aspect + move_slide | The iteration workhorse |
| 3 | `019df41a6f8777869d487aacb35d4886` | remove_tile + remove_slide | Cleanup (also unblocks "replace placeholder with real image") |
| 4 | `019df41aa8c0715f889ceaf00150a96a` | render_slide + render_thumbnail + render_tile | The "see what I built" tool — without this, no visual iteration |
| 5 | `019df41ace3b7d8fb1a132c607df0d1b` | Substrate: add `resolved: bool` to annotation envelopes | Prereq for annotation set_resolved tool below |
| 6 | `019df41afdf2757fb1139f51368701c3` | Annotation tools: add/remove/set_resolved + list_kinds + list_open | Instructions-in-annotations workflow |
| 7 | `019df41b25a27be6bc97bb7c5b50694a` | Spreadsheet tools: add_spreadsheet_tile, modify_spreadsheet_cells, resize_spreadsheet | Third tile-kind authoring (text and image done in v1) |
| 8 | `019df41b656973619677ed3c45634bc4` | Integrated image-gen: generate_image_for_tile + minerva_generate_image | Folds 3-call dance into 1; returns PNG inline so non-chat clients can evaluate |
| 9 | `019df41b993378c29afa6cf18d9e16ae` | Test: Butter Tarts v2 end-to-end | Acceptance — exercises the full closed loop |

After landing 1–4, the user can interactively author with Claude Code as the LLM (the original "next HITL" goal). 5–9 fill in completeness.

### Restart Minerva first

The currently-running Minerva booted before `MCPPresentationTools` existed, so its registered tool list lacks `presentation_*`. **App restart needed before any in-app MCP testing.** New tools will only register on next `_init_modules` pass.

## Non-blocking backlog items filed this session

- `019df1f302e77a2784ef5df3d6723b24` — Schema: structured outline levels for text tiles (replace whitespace encoding)
- `019df40decf07fbabbafcc25179c5f4b` — Schema: `stretch_mode` field on image tiles ("fill" | "keep_aspect")

Both linked to plan `019dc0bbfcc57d81b4ac1300d4923094` / DCR `019dc0bbd6937264880b1327c942d5b6` respectively.

## Cold pickup checklist

1. `git -C ~/github/Minerva status` and `git -C ~/github/plugins status` — there's uncommitted WIP on both. Ask before committing.
2. Read this file.
3. Run the headless test as a smoke check:
   ```
   godot --headless --path ~/github/Minerva/src --script test/test_mcp_presentation_tools.gd
   ```
   Expect `34 passed, 0 failed`.
4. Read `~/github/Minerva/src/Scripts/Services/MCP/Modules/MCPPresentationTools.gd` — the v1 module + the patterns the v2 tools will mimic (especially `_resolve_target`, `_commit_target`, `_find_panel_by_tab_name`).
5. Read plugin's `_SlideModel` mutators at `~/github/plugins/presentation/ui/slide_model.gd` lines 349-448 — many of the v2 modify/remove tools wire directly to these.
6. Pick up plan `019df419ce567de0b7699b3be7b6c8b5` and start with work_item `019df41a2c1e72a591a6e8bc1131f99b` (inspect tools — unblocks the rest).

## Key memory hints (saved durable; check before re-discovering)

- `019df3fa03a07e1cb1ab0fc72c631fcb` — text-tile font derives from tile.h ÷ line_count (Option A coupling)
- `019df3ed15537f55bbc5898904609cf0` — rendered tile aspect = (w/h) × slide_aspect, not just w/h
- `019df3ae052f7443a51262c190fa85b4` — SlideEditorPanel.gd:519-526 has public get_deck()/set_deck() for live MCP mutation; off-tree means duck-typed access
- `019df3b7fff871d8a063dda8c854ffdb` — SceneTree-only tests must defer load() to first process_frame for autoload-touching scripts (the gotcha that ate 30 minutes)
- `019df3b223957d46984fcf40a7353f9f` — Minerva treats GDScript inference-from-Variant as a fatal Parse Error; use explicit `: Variant` for helpers that return Variant
- `019df1f4503676bdad4fa6b77f8820de` — graphics editor: read PNG bytes via `editor.graphics_editor.layers[i].image.save_png_to_buffer()` (the result dict from `minerva_graphics_generate_iterative` only has metadata, not bytes)

## Constraints to carry forward (from prior cycles, still active)

- Always pass `project="minerva"` to docket MCP tools when working with substrate/plan IDs.
- Off-tree plugin scripts: class_name MUST start with `Presentation_`. For `extends` between sibling plugin scripts use string-path (`extends "foo.gd"`), NOT class_name.
- Plugin scripts: list new files in BOTH `~/github/plugins/<id>/manifest.json` AND the cached `~/.local/share/godot/app_userdata/Minerva/plugins/plugins.json`.
- JSON round-trip turns ints into floats — validators that use `x is int` will fail; accept whole-number floats.
- AnnotationHost capability shape is **frozen** for canvas-sync compatibility — extend, don't rename.
- Plugin annotation code never crosses into another plugin's data — only via MCP, per the substrate's trust boundary.
- LLMs see slides as RENDERED PNGs, not raw JSON (memory: `project_presentation_llm_edit_model.md`). T7 MCP `presentation_render_slide` (work_item `019df41aa8c0715f889ceaf00150a96a`) owns that path going forward.
- White-on-white text fix in R2: RichTextLabel needs `default_color` theme override; Label needs `font_color`; TextEdit needs StyleBoxFlat with white bg + dark text + cyan border. Don't regress.
- Codex's host separation pattern (panel = substrate-only host, canvas = tile-only host, coupled via `slide_rect_changed`) is load-bearing — do not collapse back to a single host.

## Paused work (not picking up — see git/docket only if needed)

- **HITL gizmo polish** `019def28e6be7e358a7a80e33014e526` — scale gizmo, rotate gizmo visual issues, save/load rotation round-trip, fullscreen rotated tiles. Orthogonal to MCP work.
- **CAD Phase B2** is `blocked` on bug `019dec49988b7091933371908d6bbb00` ("CAD callout annotations don't properly track their edge"). Latest commit `cad@622c67c`.
